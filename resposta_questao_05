function inverterString(str) {

    var novaString = "";
    
    for (var i = str.length - 1; i >= 0; i=i-1) {
        novaString =novaString + str[i];
    }
    return novaString;
}
inverterString(prompt("Digite um texto"));
